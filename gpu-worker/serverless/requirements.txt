# NOTE: torch, torchvision, torchaudio, mmcv, runpod are installed
# separately in Dockerfile with specific index URLs / pre-built binaries.

# Image/Video processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# FaceFusion dependencies (insightface installed separately in Dockerfile)

# MuseTalk dependencies (mmcv installed separately in Dockerfile)
mmpose>=0.29.0
mmdet>=3.2.0

# Utilities
python-multipart>=0.0.9
